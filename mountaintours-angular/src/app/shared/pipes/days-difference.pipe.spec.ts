import { DaysDifferencePipe } from './days-difference.pipe';

describe('DaysDifferencePipe', () => {
  it('create an instance', () => {
    const pipe = new DaysDifferencePipe();
    expect(pipe).toBeTruthy();
  });
});
